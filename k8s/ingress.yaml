apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hrms-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hrms-backend-service
            port:
              number: 80

